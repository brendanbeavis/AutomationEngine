# AutomationEngine
AutomationEngine is a windows service for scheduling and triggering regular processes. Similar to native windows scheduled tasks.











## TODO:

Populate the WIX msi wxs file to
1. Capture the output of the AutomationEngine project via a harvest into a wxs file.
2. Deploy the files to an install location such as c:\program files\automationengine
3. Create the windows service that launches the binary.
